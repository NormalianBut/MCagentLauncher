# Codex 单任务模板 v2

复制下面模板给 Codex 使用。

```text
任务名称：

目标：

背景：

涉及路径：

不得修改：

具体要求：

架构边界：
- GitHub 负责：
- Vercel 负责：
- Supabase 负责：
- MCAgent Endpoint 负责：
- Desktop Local Executor 负责：

不得违反：
1. 不把模型推理写入 Vercel 作为长期架构。
2. 不把 Supabase 当作 Minecraft 资源镜像。
3. 不让 MCAgent 直接执行本地文件操作。
4. 不默认上传完整日志。
5. 不把商业大模型作为必要运行条件。
6. 不绕过资源平台规则。
7. 不提交 secret。

验收标准：

测试命令：

安全限制：

完成后请输出：
1. 改动文件列表
2. 实现说明
3. 测试结果
4. 风险/后续建议
```
